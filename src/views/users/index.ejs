<% layout('layout') -%>


<h1> Account </h1>

<div class="card">
  <div class="card-body">
    <h5 class="card-title">Account <%= user.username  %></h5>
    <p class="card-text">
      <strong>email:</strong> <%= user.local.email  %>
    </p>

    <a href="/"
      class="btn btn-primary"> <i class="fas fa-home"></i> Home</a>
  </div>
</div>
